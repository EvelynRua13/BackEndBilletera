# Example environment variables for local development and CIDB_HOST=localhost

# These are placeholders only. Do NOT commit real secrets.DB_USER=root

DB_HOST=localhostDB_PASSWORD=secret

DB_USER=rootDB_NAME=appbilletera

DB_PASS=your_db_passwordJWT_SECRET=change-me

DB_NAME=appbilleteraPORT=3000

JWT_SECRET=your_jwt_secretCORS_ORIGINS=http://localhost:3000

PORT=3000

# Test-specific placeholders (used by tests to avoid embedding literals in code)
TEST_PWD_SHORT=pwd-short
TEST_PWD_LONG=pwd-long-123456
TEST_PWD_WRONG=wrongpass-test
TEST_PWD_CORRECT=correctpass-test
TEST_HASHED_PASS=hashedpass
